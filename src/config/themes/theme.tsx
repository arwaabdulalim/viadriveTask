import Colors from './colors';
const theme = {
  light: {
    theme: 'light',
    color: Colors.black,
    backgroundColor: Colors.white,
  },
  dark: {theme: 'dark', color: Colors.white, backgroundColor: Colors.black},
};

export default theme;
